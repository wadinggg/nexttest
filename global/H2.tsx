import styled from "styled-components"
import { font, spacing } from "../system"

const H2 = styled.h2`
    margin: ${spacing.p400} 0 ${spacing.p700};
    font-weight: 400;
    font-size: ${font.size.s700}
`

export default H2